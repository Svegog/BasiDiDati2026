% ============================================================
%  RELAZIONE --- Gestionale Stabilimento Balneare
%  Corso di Basi di Dati
%  Compilare con: pdflatex main.tex  (due volte per i riferimenti)
% ============================================================

\documentclass[12pt, a4paper, twoside]{report}

% -- Encoding e lingua ---------------------------------------
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[italian]{babel}

% -- Layout pagina -------------------------------------------
\usepackage[
  a4paper,
  top=3cm, bottom=3cm,
  inner=3.5cm, outer=2.5cm,   % margini asimmetrici (rilegatura)
  headheight=15pt
]{geometry}

% -- Tipografia ----------------------------------------------
% microtype disabilitato (richiede font scalabili non disponibili)
% \usepackage{microtype}
\usepackage{setspace}
\onehalfspacing               % interlinea 1,5

% -- Colori e grafica ----------------------------------------
\usepackage[dvipsnames, table]{xcolor}
\usepackage{graphicx}
\usepackage{float}            % [H] per le figure
\usepackage{tikz}             % diagrammi (opzionale)

% Palette colori del progetto
\definecolor{primary}{HTML}{1A3C5E}      % blu scuro intestazioni
\definecolor{accent}{HTML}{2E86AB}       % azzurro accenti
\definecolor{lightgray}{HTML}{F5F5F5}   % sfondo tabelle
\definecolor{rowalt}{HTML}{EAF4FB}       % righe alternate tabelle

% -- Intestazioni e piè di pagina ----------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE]{\small\textcolor{primary}{\leftmark}}
\fancyhead[RO]{\small\textcolor{primary}{\rightmark}}
\fancyfoot[C]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

% -- Titoli dei capitoli (stile personalizzato) --------------
\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries\color{primary}}
  {\chaptertitlename\ \thechapter}{16pt}{\Huge}
\titleformat{\section}
  {\normalfont\Large\bfseries\color{primary}}
  {\thesection}{1em}{}
\titleformat{\subsection}
  {\normalfont\large\bfseries\color{accent}}
  {\thesubsection}{1em}{}
\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries}
  {\thesubsubsection}{1em}{}

% -- Tabelle -------------------------------------------------
\usepackage{booktabs}         % linee orizzontali professionali
\usepackage{tabularx}         % colonne a larghezza variabile
\usepackage{longtable}        % tabelle multi-pagina
\usepackage{multirow}         % celle multi-riga
\usepackage{array}            % colonne personalizzate
\usepackage{colortbl}         % celle colorate

% Tipo di colonna centrato con larghezza fissa
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{R}[1]{>{\raggedleft\arraybackslash}p{#1}}

% -- Codice SQL e listati -------------------------------------
\usepackage{listings}
\usepackage{lstautogobble}

\lstdefinestyle{sql}{
  language=SQL,
  basicstyle=\ttfamily\small,
  keywordstyle=\color{primary}\bfseries,
  stringstyle=\color{accent},
  commentstyle=\color{gray}\itshape,
  numberstyle=\tiny\color{gray},
  numbers=left,
  numbersep=8pt,
  stepnumber=1,
  showstringspaces=false,
  breaklines=true,
  frame=single,
  framesep=6pt,
  rulecolor=\color{lightgray!80!black},
  backgroundcolor=\color{lightgray},
  xleftmargin=16pt,
  autogobble=true,
  morekeywords={
    CREATE, TABLE, PRIMARY, KEY, FOREIGN, REFERENCES,
    INSERT, INTO, VALUES, SELECT, FROM, WHERE, JOIN,
    INNER, LEFT, RIGHT, ON, GROUP, BY, ORDER, HAVING,
    NOT, NULL, UNIQUE, CHECK, DEFAULT, CASCADE,
    SET, UPDATE, DELETE, CONSTRAINT, INDEX,
    DATE, INTEGER, VARCHAR, DECIMAL, BOOLEAN, SERIAL
  },
  literate=
    {à}{{\`a}}1 {è}{{\`e}}1 {ì}{{\`i}}1
    {ò}{{\`o}}1 {ù}{{\`u}}1 {é}{{\'e}}1,
}
\lstset{style=sql}

% -- Matematica ----------------------------------------------
\usepackage{amsmath}
\usepackage{amssymb}

% -- Link e PDF metadata -------------------------------------
\usepackage[
  colorlinks=true,
  linkcolor=primary,
  urlcolor=accent,
  citecolor=accent,
  pdftitle={Gestionale Stabilimento Balneare --- Relazione Basi di Dati},
  pdfauthor={Autore 1, Autore 2},
  pdfsubject={Progetto di Basi di Dati},
  pdfkeywords={database, relazionale, stabilimento balneare, E/R}
]{hyperref}

% -- Didascalie figure e tabelle -----------------------------
\usepackage[
  font=small,
  labelfont={bf, color=primary},
  margin=10pt
]{caption}

% -- Indice delle figure e delle tabelle ---------------------
\usepackage{tocloft}
\renewcommand{\cftchapfont}{\bfseries\color{primary}}

% -- Ambiente per le box di nota -----------------------------
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable}

\newtcolorbox{notabox}[1][Nota]{
  colback=rowalt,
  colframe=accent,
  fonttitle=\bfseries,
  title=#1,
  breakable,
  left=6pt, right=6pt, top=4pt, bottom=4pt
}

\newtcolorbox{ambiguitabox}{
  colback=orange!8,
  colframe=orange!60!black,
  fonttitle=\bfseries,
  title=Ambiguità rilevata,
  breakable,
  left=6pt, right=6pt
}

% -- Comandi personalizzati -----------------------------------
\newcommand{\entita}[1]{\textsc{#1}}          % nomi entità in maiuscoletto
\newcommand{\attributo}[1]{\textit{#1}}        % attributi in corsivo
\newcommand{\relazione}[1]{\textsf{#1}}        % associazioni in sans-serif
\newcommand{\chiave}[1]{\underline{#1}}         % chiave primaria sottolineata
\newcommand{\chiaveest}[1]{\underline{\textit{#1}}}  % chiave esterna

% Comando per indicare cardinalità
\newcommand{\card}[2]{(#1,#2)}

% -- Spaziatura ----------------------------------------------
\setlength{\parskip}{6pt}
\setlength{\parindent}{0pt}   % nessun rientro (stile tecnico)

% ============================================================
%  INIZIO DOCUMENTO
% ============================================================
\begin{document}

% -- Frontespizio --------------------------------------------
\input{capitoli/00_frontespizio}

% -- Sommario (abstract opzionale) ---------------------------
% \input{capitoli/00_sommario}

% -- Indice generale -----------------------------------------
\tableofcontents
\clearpage

% -- Indice delle figure (opzionale) -------------------------
% \listoffigures
% \listoftables
% \clearpage

% -- Capitoli ------------------------------------------------
\input{capitoli/01_introduzione}
\input{capitoli/02_analisi_requisiti}
\input{capitoli/03_progettazione_concettuale}
\input{capitoli/04_progettazione_logica}
\input{capitoli/05_applicazione}

% -- Glossario (opzionale) -----------------------------------
% \input{capitoli/06_glossario}

\end{document}

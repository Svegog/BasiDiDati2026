% ============================================================
%  CAPITOLO 4 --- PROGETTAZIONE LOGICA
% ============================================================
\chapter{Progettazione logica}
\setlength{\headheight}{25.5pt}
\raggedbottom

% -- 4.1 Stima del volume dei dati ---------------------------
\section{Stima del volume dei dati}

La tabella seguente riporta la stima del numero di istanze per ogni entit\`{a} e
associazione, basata su un'ipotetica stagione balneare di medio-grandi dimensioni
(~200 ombrelloni, ~1000 clienti registrati).

Dato che, da richiesta del committente, si tiene uno storico degli ordini, clienti e turni,
la stima seguente è calcolata su un lasso di tempo di 5 anni.

\begin{table}[htbp]
  \centering
  \caption{Tabella dei volumi: Entità}
  \label{tab:volumi-entità}
  \rowcolors{2}{rowalt}{white}
  \begin{tabularx}{\textwidth}{L{4cm} R{1.6cm} L{9.1cm}}
    \toprule
    \rowcolor{primary!15}
    \textbf{Entità} & \textbf{Volume} & \textbf{Note} \\
    \midrule
    \entita{Proprietario}      & 5         & Soci dello stabilimento \\
    \entita{Cliente}           & 4.000     & Clienti registrati \\
    \entita{Ombrellone}        & 200       & Postazioni totali \\
    \entita{Fila}              & 10        & File totali \\
    \entita{Prenotazione}      & 11.250    & $\sim$ 15 prenotazioni per ombrellone a stagione \\
    \entita{Abbonamento}       & 250       & $\sim$ 50 abbonamenti a stagione \\
    \entita{Noleggio\_lettino} & 7.500     & $\sim$ 15 al giorno \\
    \entita{Tariffe\_noleggio} & 3         & Mattina, pomeriggio o giornata completa \\
    \entita{Dipendente}        & 60        & Personale stagionale \\
    \entita{Turno}             & 9000      & $\sim$ 90 turni per dipendente ogni stagione \\
    \entita{Mansione}          & 10        & Ruoli necessari \\
    \entita{Prodotto}          & 600       & Articoli abitualmente comprati \\
    \entita{Categoria}         & 20        & Categorie dei prodotti \\
    \entita{Fornitore}         & 20        & Fornitori abituali \\
    \entita{Catalogo}          & 1200      & In media 2 fornitori per prodotto \\
    \entita{Ordine}            & 1000      & $\sim$ 2 ordini/giorno \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{table}[H]  %[htbp]
  \centering
  \caption{Tabella dei volumi: Associazioni}
  \label{tab:volumi-associazioni}
  \rowcolors{2}{rowalt}{white}
  \begin{tabularx}{\textwidth}{L{3cm} C{2.5cm} R{1.6cm} L{7.3cm}}
    \toprule
    \rowcolor{primary!15}
    \textbf{Associazione} & \textbf{Cardinalità} & \textbf{Volume} & \textbf{Derivazione} \\
    \midrule
    Emissione             & 1:N     & 1.000   & = \entita{Ordine} \\
    Inserimento           & 1:N     & 1.000   & = \entita{Ordine} \\
    Dettaglio\_Ordine     & N:M     & 10.000  & $\sim$ 10 prodotti per \entita{Ordine} \\
    Offerta               & 1:N     & 1200    & = \entita{Catalogo} \\
    Relativa              & 1:N     & 1200    & = \entita{Catalogo} \\
    Appartenenza          & 1:N     & 600     & = \entita{Prodotto} \\
    Acquisto              & 1:N     & 7.500   & = \entita{Noleggio\_lettino} \\
    Tipologia             & 1:N     & 7.500   & = \entita{Noleggio\_lettino} \\
    Richiesta             & 1:N     & 7.500   & = \entita{Prenotazione} \\
    Riguarda              & N:M     & 9.000   & $\sim$ 1.2 \entita{Ombrelloni} per prenotazione \\
    Sottoscrizione        & 1:N     & 250     & = \entita{Abbonamento} \\
    Riservazione          & 1:N     & 250     & = \entita{Abbonamento} \\
    Locazione\_ombrellone & 1:N     & 200     & = \entita{Ombrellone} \\
    Assegnazione          & 1:N     & 9000    & = \entita{Turno} \\
    Svolgimento           & 1:N     & 9000    & = \entita{Turno} \\
    \bottomrule
  \end{tabularx}
\end{table}

% -- 4.2 Operazioni principali -------------------------------
\section{Descrizione operazioni principali e stima}

A seguito le operazioni principali richieste dal committente sul database.

\begin{itemize}[itemsep=1pt, topsep=0pt]
    \item \textbf{O.1:} \ Inserimento di un cliente
    \item \textbf{O.2:} \ Inserimento di una prenotazione
    \item \textbf{O.3:} \ Inserimento di un abbonamento
    \item \textbf{O.4:} \ Inserimento del noleggio di un lettino
    \item \textbf{O.5:} \ Registrazione nuovi dipendenti
    \item \textbf{O.6:} \ Aggiunta di un nuovo turno di lavoro
    \item \textbf{O.7:} \ Inserimento di un ordine
    \item \textbf{O.8:} \ Aggiunta di un nuovo fornitore con catalogo prodotti
    \item \textbf{O.9:} \ Registrazione consegna e aggiornamento giacenze
    \item \textbf{O.10:} Visualizzazione dipendenti in turno per data
    \item \textbf{O.11:} Verifica disponibilit\`{a} ombrelloni per intervallo
    \item \textbf{O.12:} Controllo giacenze magazzino
    \item \textbf{O.13:} Visualizzazione storico cliente
    \item \textbf{O.14:} Modifica tariffa lettini
    \item \textbf{O.15:} Modifica prezzi per fila
    \item \textbf{O.16:} Aggiornamento prezzi catalogo di un fornitore
    \item \textbf{O.17:} Aggiornamento giacenze dopo inventario
    \item \textbf{O.18:} Inserimento conferma pagamento
\end{itemize}

\begin{table}[H]
  \centering
  \caption{Frequenza operazioni principali}
  \label{tab:operazioni}
  \rowcolors{2}{rowalt}{white}
  \begin{tabularx}{\textwidth}{C{2cm} C{0.6cm} C{2.8cm} C{2cm} C{0.6cm} C{2.8cm} C{2cm}}
  \toprule
  \rowcolor{primary!15}
  & \textbf{N.} & \textbf{Frequenza} & & \textbf{N.} & \textbf{Frequenza} & \\
  \midrule
   & O.1 & 10/giorno     & & O.10 & 5/giorno     & \\
   & O.2 & 25/giorno     & & O.11 & 30/giorno    & \\
   & O.3 & 50/stagione   & & O.12 & 2/giorno     & \\
   & O.4 & 15/giorno     & & O.13 & 3/giorno     & \\
   & O.5 & 12/stagione   & & O.14 & 3/stagione   & \\
   & O.6 & 15/giorno     & & O.15 & 10/stagione  & \\
   & O.7 & 2/giorno      & & O.16 & 2/settimana  & \\
   & O.8 & 2/giorno      & & O.17 & 1/giorno     & \\
   & O.9 & 2/giorno      & & O.18 & 45/giorno    & \\
  \bottomrule
  \end{tabularx}
\end{table}

\clearpage

% -- 4.3 Schemi di navigazione -------------------------------
\section{Schemi di navigazione e tabelle degli accessi} % ONGOING

\subsection{O.1 --- Inserimento cliente}

Questa operazione inserisce un nuovo cliente nel Database.

\begin{table}[H]
  \centering
  \caption{Tabella degli accessi --- O2: Disponibilit\`{a} ombrelloni}
  \label{tab:accessi-o2}
  \rowcolors{2}{rowalt}{white}
  \begin{tabularx}{\textwidth}{L{4cm} C{2cm} C{2cm} C{2cm} C{2cm}}
    \toprule
    \rowcolor{primary!15}
    \textbf{Concetto} & \textbf{Tipo} & \textbf{L/S} & \textbf{Accessi} & \textbf{Tot/mese} \\
    \midrule
    \entita{Cliente}   & E & S & 200    & 300.000 \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsection{O3 --- Dipendenti in turno per una data specifica}

\begin{table}[H]
  \centering
  \caption{Tabella degli accessi --- O3: Dipendenti in turno}
  \label{tab:accessi-o3}
  \rowcolors{2}{rowalt}{white}
  \begin{tabularx}{\textwidth}{L{4cm} C{2cm} C{2cm} C{2cm} C{2cm}}
    \toprule
    \rowcolor{primary!15}
    \textbf{Concetto} & \textbf{Tipo} & \textbf{L/S} & \textbf{Accessi} & \textbf{Tot/mese} \\
    \midrule
    \entita{Turno}      & E & L & 1.800  & 27.000 \\
    \entita{Dipendente} & E & L & 20     & 300 \\
    \relazione{LavoraIn}& A & L & 1.800  & 27.000 \\
    \bottomrule
  \end{tabularx}
\end{table}

% -- 4.4 Analisi delle ridondanze ----------------------------
\section{Analisi delle ridondanze}

\begin{itemize}
  \item \textbf{Ridondanza:} l'attributo \attributo{prezzoTotale} in
        \entita{Prenotazione} \`{e} calcolabile come:
        \[
          \text{prezzoTotale} = \text{tariffaGiornaliera}(\text{fila}) \times
          (\text{dataFine} - \text{dataInizio}) \times (1 - \text{sconto})
        \]

        \textbf{Decisione:} si mantiene l'attributo ridondante \attributo{prezzoTotale}
        perché la tariffa potrebbe variare nel tempo e si vuole registrare il prezzo
        concordato al momento della prenotazione. Questo evita problemi di consistenza
        se le tariffe cambiano.
  \item Per lo stesso motivo della ridondanza precedente viene mantenuto anche
        l'attributo \attributo{Prezzo} in \entita{Abbonamento}
  \item \textbf{Ridondanza candidata:} l'attributo \attributo{PrezzoAlMomento}
        in \entita{Dettaglio Ordine}.

        \textbf{Decisione:} l'attributo ridondante \attributo{PrezzoAlMomento}
        è mantenuto perché il prezzo di un determinato \entita{prodotto},
        venduto da un \entita{Fornitore}, potrebbe cambiare nel tempo.
\end{itemize}



% -- 4.5 Raffinamento schema ---------------------------------
\section{Raffinamento dello schema}

\subsection{Eliminazione degli identificatori esterni}
Nessuna entit\`{a} nello schema utilizza identificatori esterni: tutte le entit\`{a}
sono identificate da una chiave surrogata (\texttt{id} intero auto-incrementale).

\subsection{Gerarchie}
\`{E} presente nello schema una sola gerarchia dove l'entità madre è \entita{Persona}
e le entità figlie sono \entita{Proprietario}, \entita{Cliente} e \entita{Dipendente}.
La copertura di tale gerarchia è totale ed esclusiva quindi elimino la gerarchia facendo
un collasso verso il basso dato che gli accesi alle entità figlie sono separati.
Questo tipo di eliminazione mi permette di mantenere i vincoli sulle associazioni e 
allo stesso tempo, dato che la copertura è esclusiva, non introduce ridondanza.

\subsection{Scelta delle chiavi primarie}
Nello schema ER sono già evidenziate in modo non ambiguo le chiavi primarie della maggior parte 
delle entità. Gli identificatori delle 3 relazioni create a seguito del collasso verso
il basso della gerarchia persona diventeranno dei nuovi codici surrogati in quanto 
l'email non è una caratteristica fissa di una pesona ma potrebbe cambiare nel tempo.

% -- 4.6 Traduzione in relazioni -----------------------------
\section{Traduzione di entit\`{a} e associazioni in relazioni}

\begin{longtable}{L{4.3cm} L{10cm}}
  \caption{Traduzione dello schema E/R in relazioni}
  \label{tab:traduzione} \\
  \toprule
  \rowcolor{primary!15}
  \textbf{Relazione} & \textbf{Schema (PK sottolineata, FK in corsivo)} \\
  \midrule
  \endfirsthead
  \toprule
  \rowcolor{primary!15}
  \textbf{Relazione} & \textbf{Schema} \\
  \midrule
  \endhead
  \bottomrule
  \endfoot

  PROPRIETARIO   & (\chiave{idProprietario}, nome, cognome, email, password) \\[4pt]
  CLIENTE        & (\chiave{id}, nome, cognome, telefono, email) \\[4pt]
  OMBRELLONE     & (\chiave{id}, fila, numero, numeroLettini, numeroSedie) \\[4pt]
  PRENOTAZIONE   & (\chiave{id}, \chiaveest{idCliente}, \chiaveest{idOmbrellone},
                   dataInizio, dataFine, prezzoTotale) \\[4pt]
  ABBONAMENTO    & (\chiave{id}, \chiaveest{idCliente}, \chiaveest{idOmbrellone},
                   dataInizio, dataFine, prezzo, tipo) \\[4pt]
  DIPENDENTE     & (\chiave{id}, nome, cognome, codiceFiscale, telefono, email) \\[4pt]
  TURNO          & (\chiave{id}, \chiaveest{idDipendente}, data,
                   orarioInizio, orarioFine) \\[4pt]
  CATEGORIA      & (\chiave{CodCategoria}, NomeCategoria, Descrizione*) \\ [4pt]
  PRODOTTO       & (\chiave{CodProdotto}, nome, quantità, quantitàMinima, \chiaveest{CodCategoria}:CATEGORIA) \\[4pt]
  FORNITORE      & (\chiave{PartitaIVA}, Nome, Email, RecapitoTelefonico, Indirizzo) \\[4pt]
  ORDINE         & (\chiave{CodOrdine}, \chiaveest{PartitaIVA}:FORNITORE, \chiaveest{idProprietario}:PROPRIETARIO,
                   dataOrdine, ConsegnaPrevista, DataConsegna*) \\[4pt]
  CATALOGO       & (\chiave{\chiaveest{CodProdotto}:PRODOTTO, \chiaveest{PartitaIVA}:FORNITORE}, prezzoUnitario) \\ [4pt]
  DETTAGLIO\_ORDINE & (\chiave{\chiaveest{(PartitaIVA, CodProdotto)}:CATALOGO, \chiaveest{CodOrdine}:ORDINE}, PrezzoAlMomento) \\[4pt]
\end{longtable}

% -- 4.7 Schema relazionale finale ---------------------------
\section{Schema relazionale finale}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{immagini/04/schemaLogico.png}
  \caption{Schema relazionale finale}
  \label{fig:schema-relazionale}
\end{figure}

% -- 4.8 SQL: CREATE TABLE ------------------------------------
\section{Costruzione delle tabelle in SQL}

\begin{lstlisting}[caption={Creazione delle tabelle principali},
                   label={lst:create-tables}]
create table ABBONAMENTO (
     CodFila int not null,
     NumOmbrellone int not null,
     Anno int not null,
     Prezzo decimal(8,2) not null,
     Sconto float(1) not null,
     Pagamento char not null,
     IdCliente char(1) not null,
     constraint IDABBONAMENTO primary key (CodFila, NumOmbrellone, Anno));

create table CATALOGO (
     CodProdotto int not null,
     PartitaIVA char(11) not null,
     PrezzoUnitario decimal(8,2) not null,
     constraint IDFORNITURA primary key (PartitaIVA, CodProdotto));

create table CATEGORIA (
     CodCategoria int not null auto_increment,
     NomeCategoria varchar(40) not null,
     Descrizione varchar(255),
     constraint IDCATEGORIA primary key (CodCategoria));

create table CLIENTE (
     IdCliente char(1) not null,
     Nome varchar(30) not null,
     Cognome varchar(30) not null,
     Email varchar(50) not null,
     RecapitoTelefonico varchar(15) not null,
     constraint IDCLIENTE primary key (IdCliente));

create table DETTAGLIO_ORDINE (
     CodOrdine int not null,
     PartitaIVA char(11) not null,
     CodProdotto int not null,
     Quantità int not null,
     PrezzoAlMomento decimal(8,2) not null,
     constraint IDDettaglio_Ordine primary key (CodOrdine, PartitaIVA, CodProdotto));

create table DIPENDENTE (
     IdDipendente char(1) not null,
     Nome varchar(30) not null,
     Cognome varchar(30) not null,
     Email varchar(50) not null,
     RecapitoTelefonico varchar(15) not null,
     CodiceFiscale char(16) not null,
     constraint IDDIPENDENTE primary key (IdDipendente));

create table FILA (
     CodFila int not null,
     TariffaGiornaliera decimal(8,2) not null,
     TariffaStagionale char(1) not null,
     constraint IDFILA primary key (CodFila));

create table FORNITORE (
     PartitaIVA char(11) not null,
     Nome varchar(50) not null,
     Email varchar(50) not null,
     Indirizzo varchar(60) not null,
     RecapitoTelefonico varchar(15) not null,
     constraint IDFornitore_ID primary key (PartitaIVA));

create table MANSIONE (
     CodMansione int not null auto_increment,
     NomeMansione varchar(40) not null,
     Descrizione varchar(255) not null,
     constraint IDMANSIONE_ID primary key (CodMansione),
     constraint IDMANSIONE_1 unique (NomeMansione));

create table NOLEGGIO_LETTINO (
     CodNoleggio int not null auto_increment,
     Data date not null,
     Quantità int not null,
     Pagamento char not null,
     IdCliente char(1) not null,
     TipoNoleggio varchar(15) not null,
     constraint IDNOLEGGIO_LETTINO primary key (CodNoleggio));

create table OMBRELLONE (
     CodFila int not null,
     NumOmbrellone int not null,
     constraint IDOMBRELLONE primary key (CodFila, NumOmbrellone));

create table ORDINE (
     CodOrdine int not null auto_increment,
     DataOrdine date not null,
     ConsegnaPrevista date not null,
     DataConsegna date,
     IdProprietario int not null,
     PartitaIVA char(11) not null,
     constraint IDOrdine primary key (CodOrdine));

create table PRENOTAZIONE (
     CodPrenotazione int not null auto_increment,
     DataInizio date not null,
     DataFine date not null,
     Prezzo decimal(8,2) not null,
     Sconto float(1) not null,
     Note varchar(255),
     Pagamento char not null,
     IdCliente char(1) not null,
     CodFila int not null,
     NumOmbrellone int not null,
     constraint IDPRENOTAZIONE primary key (CodPrenotazione));

create table PRODOTTO (
     CodProdotto int not null auto_increment,
     NomeProdotto char(40) not null,
     QuantitàMagazzino int not null,
     QuantitàMinima int not null,
     CodCategoria int not null,
     constraint IDProdotto_ID primary key (CodProdotto));

create table PROPRIETARIO (
     IdProprietario int not null auto_increment,
     Nome varchar(30) not null,
     Cognome varchar(30) not null,
     Email varchar(50) not null,
     RecapitoTelefonico varchar(15) not null,
     Password varchar(255) not null,
     constraint IDPROPRIETARIO primary key (IdProprietario));

create table TARIFFE_NOLEGGIO (
     TipoNoleggio varchar(15) not null,
     Prezzo decimal(8,2) not null,
     constraint IDTARIFFE_NOLEGGIO primary key (TipoNoleggio));

create table TURNO (
     IdDipendente char(1) not null,
     Data date not null,
     OraInizio char(5) not null,
     OraFine char(5) not null,
     CodMansione int not null,
     constraint IDTURNO primary key (IdDipendente, Data, OraInizio));
\end{lstlisting}

% -- 4.9 Query SQL -------------------------------------------
\section{Traduzione delle operazioni in query SQL}

\subsection{O2 --- Ombrelloni disponibili in un intervallo di date}

\begin{lstlisting}[caption={O2: Ombrelloni disponibili per una data}]
-- Parametri: :data_inizio, :data_fine
SELECT o.id, o.fila, o.numero, o.numeroLettini, o.numeroSedie
FROM   Ombrellone o
WHERE  o.id NOT IN (
    SELECT p.idOmbrellone
    FROM   Prenotazione p
    WHERE  p.dataInizio <= :data_fine
    AND    p.dataFine   >= :data_inizio
)
ORDER BY o.fila, o.numero;
\end{lstlisting}

\subsection{O3 --- Dipendenti in turno per una data specifica}

\begin{lstlisting}[caption={O3: Dipendenti in turno per una data}]
-- Parametro: :data
SELECT d.nome, d.cognome, t.orarioInizio, t.orarioFine
FROM   Dipendente d
JOIN   Turno t ON t.idDipendente = d.id
WHERE  t.data = :data
ORDER BY t.orarioInizio;
\end{lstlisting}

\subsection{O5 --- Prodotti sotto la soglia minima in magazzino}

\begin{lstlisting}[caption={O5: Mancanze in magazzino}]
SELECT p.nome, p.quantita, p.sogliaMinimaRiordino,
       (p.sogliaMinimaRiordino - p.quantita) AS quantita_mancante
FROM   Prodotto p
WHERE  p.quantita < p.sogliaMinimaRiordino
ORDER BY quantita_mancante DESC;
\end{lstlisting}

\subsection{O1 --- Inserimento nuova prenotazione}

\begin{lstlisting}[caption={O1: Inserimento prenotazione con calcolo prezzo}]
-- Inserisce una nuova prenotazione
-- Il prezzo totale viene passato dall'applicazione dopo il calcolo
INSERT INTO Prenotazione (idCliente, idOmbrellone,
                          dataInizio, dataFine, prezzoTotale)
VALUES (:id_cliente, :id_ombrellone,
        :data_inizio, :data_fine, :prezzo_totale);
\end{lstlisting}

\subsection{O8 --- Storico prenotazioni di un cliente}

\begin{lstlisting}[caption={O8: Storico prenotazioni di un cliente}]
-- Parametro: :id_cliente
SELECT o.fila, o.numero, p.dataInizio, p.dataFine, p.prezzoTotale
FROM   Prenotazione p
JOIN   Ombrellone o ON o.id = p.idOmbrellone
WHERE  p.idCliente = :id_cliente
ORDER BY p.dataInizio DESC;
\end{lstlisting}
